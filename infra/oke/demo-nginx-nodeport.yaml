apiVersion: v1
kind: Service
metadata:
  name: demo-web
  namespace: default
spec:
  type: NodePort

  selector:
    app: demo-web

  ports:
  - name: https
    port: 443
    targetPort: 443
    nodePort: 30443
